{
  "name": "guide01-ts-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser demo driving a Guide01 device over Web Bluetooth using the @guide01/protocol WASM bindings.",
  "scripts": {
    "predev": "npm --prefix .. install && npm --prefix .. run build",
    "dev": "vite",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "prebuild": "npm --prefix .. install && npm --prefix .. run build",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@guide01/protocol": "file:.."
  },
  "devDependencies": {
    "@types/web-bluetooth": "^0.0.21",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
